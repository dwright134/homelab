apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cluster-domain-certificate
  namespace: networking
spec:
  secretName: cluster-domain-certificate-secret
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - "*.home.danobytes.com"
